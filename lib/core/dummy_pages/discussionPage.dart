import 'package:flutter/material.dart';

class DiscussingPage extends StatelessWidget {
  const DiscussingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [Text("DiscussionPage")],
        ),
      ),
    );
  }
}
